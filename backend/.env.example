# Server (Railway sets PORT automatically)
PORT=4000
NODE_ENV=development

# Database – Railway: use Postgres plugin and copy DATABASE_URL
# Vercel: use Vercel Postgres or external Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/amanah_institute"

# Auth (set JWT_SECRET in Railway / Vercel env)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Optional: future email, SMS, etc.
# SENDGRID_API_KEY=
# TWILIO_ACCOUNT_SID=
